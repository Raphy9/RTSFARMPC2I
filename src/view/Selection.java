package src.view;

import src.model.Camera;
import src.model.World;

/**
 * Classe Selection est responsable de la sélection d'une tuile dans le jeu
 */
public class Selection extends Global {


    public Selection(World world, Camera camera) {
        super(world, camera);
    }
}
